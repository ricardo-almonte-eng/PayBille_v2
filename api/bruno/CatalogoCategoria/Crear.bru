meta {
  name: Crear
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/catalogocategoria
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "nombre": "Celulares",
    "idCategoriaPadre": null,
    "icono": "smartphone",
    "activa": true
  }
}

script:post-response {
  if (res.body.code === "0") {
    bru.setVar("catalogoCategoriaId", res.body.data.id);
  }
}
